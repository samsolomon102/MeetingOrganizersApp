json.extract! meeting_organizer_list, :id, :employee_id, :first_name, :last_name, :email_id, :last_used, :created_at, :updated_at
json.url meeting_organizer_list_url(meeting_organizer_list, format: :json)
